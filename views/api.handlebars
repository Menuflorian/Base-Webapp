

<h2 class="page-header">Api</h2>






<script LANGUAGE="Javascript">
    var ListProjet = JSON.parse(decodeURI("{{ dbData }}" ));
    console.log(ListProjet);

    var LpLength = Object.keys(ListProjet).length

    console.log(ListProjet.length);


    ListProjet.forEach(function(element) {
      var name = element.name;
      var corp = element.corp;
      var text = " ";
      var id = element._id;
      document.write(name);
      document.write(text);
      document.write(corp);
      document.write(text);
      document.write('<button type="button" class="btn btn-outline-success" value= "id">Delete</button>');
      document.write('<br />');
      return {element};
        });
</script>
