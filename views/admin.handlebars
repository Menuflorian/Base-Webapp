<div class="container">
    {{> breadcrumb}}
    <div class="row">
        {{> admin-link}}
        <h1 class="">Index admin
        {{#if admin}}
         	<small>You are an admin.</small>
        {{/if}}
    </h1>
    </div>
</div>
