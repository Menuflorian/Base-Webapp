{{> layout-head}}
{{> navbar}}
{{> error-alert}}
<div id="divalert"></div>
<div class="container">
    {{> admin-breadcrumb}}
    <div class="row">
        {{>admin-link}}
        {{{body}}}
    </div>
</div>
{{> layout-foot}}
